import { test } from "@playwright/test";

test.describe("Verify AGS can make a registration renewable by updating Next Due Date / Expiration Date and taxpayer can then start renewal.", () => {
  test.skip("Initiating test", async () => {});
});
