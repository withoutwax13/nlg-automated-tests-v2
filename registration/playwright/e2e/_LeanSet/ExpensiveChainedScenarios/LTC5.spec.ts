import { test } from "@playwright/test";

test.describe("Verify taxpayer can perform multi-location renewal changes by adding a new location and de-registering an existing location in the same renewal, and the approved result reflects the de-registered location status.", () => {
  test.skip("Initiating test", async () => {});
});
