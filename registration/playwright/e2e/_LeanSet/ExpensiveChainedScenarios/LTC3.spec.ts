import { test } from "@playwright/test";

test.describe("Verify taxpayer can submit a non-listed registration and after AGS review/approval/payment the certificate is absent on all relevant surfaces when eligibility conditions are not met.", () => {
  test.skip("Initiating test", async () => {});
});
