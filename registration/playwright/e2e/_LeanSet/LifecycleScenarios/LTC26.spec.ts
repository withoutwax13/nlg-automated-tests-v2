import { test } from "@playwright/test";

test.describe("Verify AGS approval payment action menu is correct across the lifecycle (Not Paid -> Fully Paid -> Refunded -> Declined), and that Not Applicable has no action menu.", () => {
  test.skip("Initiating test", async () => {});
});
