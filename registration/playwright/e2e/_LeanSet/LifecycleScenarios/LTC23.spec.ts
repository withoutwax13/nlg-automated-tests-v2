import { test } from "@playwright/test";

test.describe("Verify Government registration status action menu is correct across the lifecycle (Not Registered -> Active -> Revoked -> De-Registered.)", () => {
  test.skip("Initiating test", async () => {});
});
